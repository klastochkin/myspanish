"""Static user definitions. Part of the deployment.

Passwords are NOT stored here — they live in passwords.json (git-ignored),
set by each user on their first login.
"""

USERS = {
    "F": {"role": "user"},
    "D": {"role": "supervisor"},
}
